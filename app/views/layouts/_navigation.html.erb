<div class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Scoutshout", home_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? && current_user %>
          <li><%= link_to "Setcard", setcard_path(current_user)  %></li>
          <li><%= link_to "Experience", "/#" %></li>
          <li><%= link_to "<i class='fa fa-search'></i> &nbsp; Find Model".html_safe, find_model_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? && current_user %>
          <li><%= link_to current_user.email, setcard_path(current_user) %></li>
          <li><%= link_to ('<i class="fa fa-calendar"></i>').html_safe,
            "/#", title: 'Calendar' %></li>
          <li><%= link_to ('<i class="fa fa-bell"></i>').html_safe,
            "/#", title: 'Notification' %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to ('<i class="fa fa-user"></i> ' + "Edit profile").html_safe,
            edit_user_registration_path, title: 'Edit profile' %></li>
              <li><%= link_to ('<i class="fa fa-power-off"></i> ' + "Logout").html_safe, destroy_user_session_path,  title: 'Logout', :method => :delete %></li>
            </ul>
          </li>
          <% else %>
          <li><%= link_to('Sign In', new_user_session_path) %></li>
          <li><%= link_to('Sign Up', new_user_registration_path) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>